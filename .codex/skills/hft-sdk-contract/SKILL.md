---
name: hft-sdk-contract
description: HFT 接口契约 skill。读取本地 HFT SDK 接口文档，并把它转换成页面生成契约。这个项目中的页面默认都要依据 `references/hft-sdk-api-reference.md` 生成；只有当页面名、模块、动作和接口能力都能在文档中检索到时，才能进入正式生成。字段如果不在文档中，则明确告知用户该字段未找到并在页面中省略；页面能力如果不在文档中，则直接说明当前系统不支持。
---

# HFT SDK 契约映射

本项目中的 HFT 页面生成默认使用这个 skill。

## 阅读顺序

1. 读取 [hft-sdk-api-reference.md](/D:/hft-ai-agent/.codex/skills/hft-sdk-contract/references/hft-sdk-api-reference.md)。
2. 读取 [hft-sdk-view.md](/D:/hft-ai-agent/.codex/skills/hft-sdk-contract/references/hft-sdk-view.md) 作为精简摘要。
3. 检索用户要求的页面名、模块名、动作名和字段名。
4. 只抽取与当前页面请求直接相关、且能在文档中找到依据的接口。

## 需要提取的内容

- path
- method
- 请求对象名称
- 请求字段
- 默认值
- 响应结构
- 需要展示的响应字段
- 相关 SSE 事件名称
- 前端如何组装请求
- 前端收到响应后如何更新 DOM
- 用户要求但文档中未找到的字段
- 用户要求但文档中未找到的页面模块或页面名称

## 映射规则

- 不要发明接口文档中不存在的端点。
- 不要发明无法从文档追溯的业务字段、表格列、筛选项或操作。
- 字段不在文档中时，不展示该字段，并明确告知用户该字段未在接口文档中找到。
- 文档中给出的默认值，应优先作为 HTML 表单默认值。
- 前端实现必须由契约反推真实调用函数，例如查询函数、提交函数、订阅函数、断开函数、渲染函数。
- 首版页面就要落成真实调用，不要先生成静态原型页。
- 页面名本身也必须可追溯；例如用户要求“双边报单面”，只有在文档中能检索到对应依据时，才允许继续生成。
- 如果整个页面能力不在文档中，应直接标记为“当前系统不支持”。

## 输出契约格式

输出给下游生成步骤时，至少包含：

- HTTP 基础地址
- SSE 地址模式
- 已选择接口
- 这些接口推导出的页面区域
- 字段默认值
- 响应结构
- 展示字段
- 省略字段清单
- 事件订阅
- 页面里的真实函数映射
- 不支持项清单

## 项目默认地址

除非用户另行说明，默认使用：

- HTTP base: `http://172.24.17.44:9005/hft-sdk`
- SSE stream: `http://172.24.17.44:9005/hft-sdk/sse/stream?clientId={clientId}`